# apiVersion: agentgateway.dev/v1alpha1
# kind: AgentgatewayPolicy
# metadata:
#   name: rbac-policy
#   namespace: agentgateway-system
# spec:
#   targetRefs:
#     - group: gateway.networking.k8s.io
#       kind: HTTPRoute
#       name: anthropic
#   traffic: 
#     authorization: 
#       action: Allow
#       policy: 
#         matchExpressions:
#           - "request.headers['x-llm'] == 'anthropic'"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: anthropic-prompt-guard
  namespace: agentgateway-system
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: anthropic
  backend:
    ai:
      promptGuard:
        request:
        - webhook:
            backendRef: 
              kind: Service
              name: ai-guardrail-webhook
              port: 8000
        response:
        - webhook:
            backendRef: 
              kind: Service
              name: ai-guardrail-webhook
              port: 8000